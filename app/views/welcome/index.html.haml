- title "Welcome"

%p
  %h1 Welcome to ClearCATS!
  
  #projects
    %h3 Initiate Service
    %ul
      - OrganizationalUnit.all(:order => :name).each do |ou|
        %li
          = ou.name
          - if !ou.service_lines.blank?
            %ul
              - ou.service_lines.all(:order => "name").each do |svc_line|
                %li 
                  = link_to svc_line.name, :controller => "services", :action => "new", :service_line_id => svc_line.id
          
