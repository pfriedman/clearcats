- stylesheet "services"
- javascript "jquery.blockUI", "modal_form"
- title "New Service - Choose Publications"

= render "menu"

%p
  Publications PMCIDs that resulted from support from this program.
  
= render '/publications/list', :service => @service, :publications => @publications, :person => @service.person, :search_params => @search_params

- form_for @service do |f|
  = hidden_field_tag "service[state]", "choose_approvals"
  = f.submit "Continue", :disable_with => 'Continuing...'

#modal_window