- stylesheet "people"
- title "Investigators"

:javascript
  $(document).ready(function() {
    $(".help_icon").tooltip();
  });

- if File.exists?(Person.import_error_log(current_user.username))
  .page_section
    #upload_error_log
      = link_to "View Upload Errors", {:controller => "welcome", :action => "upload_error_log"}, { :class => "show_link icon_link" }

.page_section 
  = render 'search_form'

.page_section
  = render 'search_results'
