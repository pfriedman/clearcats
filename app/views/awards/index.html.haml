- stylesheet "people", "awards", "services"
- javascript "jquery.blockUI", "modal_form"
- title "Awards for #{@person}"

:javascript
  $(function () { $(".datepicker").live('mouseover', function() { $(this).datepicker(); }); });

= render "/people/client_menu" if faculty_member?

.page_section
  = render '/awards/list', :awards => @awards, :person => @person, :search => @search_params, :service => nil

#modal_window