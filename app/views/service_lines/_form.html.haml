- stylesheet "service_lines"

- form_for(@service_line) do |f|
  = f.error_messages

  %p
    = f.label :name 
    %br
    = f.text_field :name
  
  %p
    = f.label :organizational_unit_id, "Organizational Unit"
    %br
    - org_unit = @user_organizational_units.size == 1 ? @user_organizational_units.first : OrganizationalUnit 
    = f.select :organizational_unit_id, options_for_select(nested_set_options(org_unit) {|i| "#{'-' * i.level} #{i.name} (#{i.abbreviation})" },  @service_line.organizational_unit_id)
  
  %table{ :class => "records activity_types" }
    %thead
      %tr
        %th
          Activity Types
        %th
    %tbody{ :class => "nested_records" }
      - f.fields_for :activity_types do |activity_type_form|
        = render 'activity_type_fields', :f => activity_type_form
    %tfoot
      %tr
        %td{ :colspan => '2' }
          = link_to_add_fields "Add Activity Type", f, :activity_types
  %p
    = f.submit "Save", :disable_with => 'Saving...'